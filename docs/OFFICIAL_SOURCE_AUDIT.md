# 공식 출처 매칭 감사

2026-09-19 UTC에 원격 D1 `weekend-mwohae-production`에 저장된 TourAPI 행사 263건을 감사했다. 최신 동기화 대상은 244건이며, 이전에 저장된 19건은 삭제하지 않고 보존된 상태에서 상세정보 미확보로 구분했다. 감사 run은 `official-1ee80f67c3fb0955d4e0087a`다.

이번 변경은 기존 `events`, TourAPI `sources`, 공개 노출에 사용되는 `event_evidence`를 수정하지 않는다. `official_source_audits`는 행사별 원본 기준값·상세 응답·URL 목록을, `official_source_links`는 URL별 접근 결과·본문 제목·hash·검토 근거를, `official_source_comparisons`는 필드별 비교 결과를 저장한다. 한 행사에 여러 유형을 연결할 수 있고, HTTP 접근 실패는 공식성 판정으로 사용하지 않는다. 최종 사용자용 `verified`·`stale`·변경 상태 로직도 변경하지 않았다.

## 전체 측정

| 항목 | 고유 행사 수 | 레코드 수 또는 설명 |
| --- | ---: | --- |
| 감사 대상 전체 | 263 | 감사 레코드 263 |
| 최신 TourAPI 스냅샷 | 244 | 이전 보존 19건 포함 전체 기준 |
| TourAPI raw payload의 행사 관련 URL | 0 | 목록 원문에는 홈페이지가 없었고 이미지 URL만 존재 |
| TourAPI raw payload에 URL이 있는 행사(이미지 포함) | 263 | 이미지 URL은 공식 출처 후보에서 제외 |
| 상세정보까지 포함한 행사 관련 URL 보유 | 209 | 고유 후보 URL 228, 연결 레코드 229 |
| 공식 출처 1개 이상 확인 | 107 | 공식 출처 링크 레코드 107 |
| 행사 공식 홈페이지 | 72 | `event_official`을 포함한 고유 행사 |
| 주최·주관기관 공식 출처 | 61 | `organizer_official`을 포함한 고유 행사 |
| 지자체·공공기관 공식 출처 | 26 | `local_government`를 포함한 고유 행사 |
| 한국관광공사 페이지만 확인 | 2 | `visitkorea`만 공식 후보인 행사 |
| 기타 URL만 있는 행사 | 6 | `other`만 연결된 행사 |
| 공식 출처 미확인 | 156 | 접근 가능하지만 관계를 확정할 근거가 없거나 후보 없음 |

유형별 고유 행사 수는 여러 유형 연결 때문에 합계가 전체 행사 수와 일치하지 않는다. 블로그·카페·SNS·티켓 판매처는 최종 공식 출처로 인정하지 않았고, Notion처럼 행사와 운영기관 관계를 확인할 수 없는 페이지도 미확인으로 남겼다.

## 접근 결과

후보 URL은 228개를 조회했다. 성공 158건, HTTP 오류 47건(429 35, 403 7, 404 3, 400 1, 405 1), 네트워크/리디렉션 실패 22건, 응답이 너무 큰 페이지 1건이다. 접근 실패 링크가 연결된 고유 행사는 63건이다. 이 상태는 비공식 판정이 아니라 별도 접근 상태로 저장했다.

## 비교 결과

공식 페이지에서 해당 회차의 명시적 문구를 확인한 경우만 비교했다. 기존 `events` 값은 자동 수정하지 않았다.

| 필드 | 일치 | 불일치 | 비교 불가·미확인 |
| --- | ---: | ---: | ---: |
| 행사명 | 73 | 0 | 190 |
| 시작일 | 33 | 0 | 230 |
| 종료일 | 33 | 0 | 230 |
| 장소 | 18 | 1 | 244 |
| 주소 | 2 | 0 | 261 |
| 요금 | 2 | 0 | 261 |
| 취소 | 0 | 0 | 263 |
| 연기 | 0 | 0 | 263 |
| 운영 중단·변경 | 0 | 0 | 263 |

불일치 1건은 `tourapi-4113071` 문경 가은아자개 장터축제의 장소다. TourAPI는 `문경 에코월드 주차장`, 공식 페이지는 `가은아자개장터 일원`으로 명시한다. 근거 URL과 원문 일부는 `official_source_comparisons`에 저장했으며 기존 행사 값은 유지했다.

공식 페이지에 해당 회차의 필드가 명확히 없으면 불일치로 세지 않았다. 예매기간·개막식·지난 회차가 함께 표시되는 페이지는 행사 본 일정과 혼동하지 않았다.

## 검증

- `npm test`: 단위 12개와 D1 통합 검사 통과.
- `npm run build`: React 정적 빌드 통과.
- 실제 데이터 브라우저 회귀: 공개 URL 데스크톱·모바일 12개 통과. 로컬 재검사는 D1 CLI와 로컬 서버 동시 접근으로 일시적인 503이 발생한 뒤 서버를 중지하고 D1 저장 검사를 단독 재실행했다.
- 로컬·원격 D1 감사 저장 검증: 행사 263, TourAPI sources 263, 기존 TourAPI evidence 1,052건이 저장 전후 동일; 감사 263, 링크 229, 비교 2,367; 외래키 오류 0.

공식 출처 데이터는 감사·후속 변경 감지 기반으로만 저장했다. 이번 단계에서 AI 추천·알림·날짜 선택·장기 행사 수집·자동 신뢰상태 판정은 추가하지 않았다.
